/** Page size for the main commit history graph. Matches the legacy hard cap. */
export const HISTORY_PAGE_SIZE = 200;

/** Page size for the per-file history workspace. */
export const FILE_HISTORY_PAGE_SIZE = 100;
